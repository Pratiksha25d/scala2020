class sample_maths {

  def operations(c:Int, a:Int, b:Int)={
    if(c==1)
      a + b
    else if(c==2)
      a - b
    else if(c==3)
      a / b
    else
      a * b
  }
  println("object created")
}
